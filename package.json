{
  "name": "excel-processor",
  "version": "1.0.0",
  "description": "Simple web app to process Excel files",
  "main": "index.html",
  "scripts": {
    "start": "npx live-server --port=3000"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
